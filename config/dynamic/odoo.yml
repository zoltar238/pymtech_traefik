http:
  routers:
    odoo:
      rule: "Host(`odoo.testing.com`)"
      entryPoints:
        - web  # Solo HTTP
      service: odoo-service

  services:
    odoo-service:
      loadBalancer:
        servers:
          - url: "http://192.168.56.32:8069"
        healthCheck:
          path: /web/database/selector
          interval: 30s
          timeout: 5s